name: Fix player daily state and fluidity 20260902

on:
  push:
    branches: [react-vite-migration]
    paths:
      - '.github/workflows/fix-player-daily-state-fluidity-20260902.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: react-vite-migration
          fetch-depth: 0

      - name: Apply daily state and fluidity fixes
        run: |
          python - <<'PY'
          from pathlib import Path

          def replace_once(path, old, new, label):
              p = Path(path)
              s = p.read_text()
              if old not in s:
                  raise SystemExit(f'Anchor not found for {label}: {path}')
              p.write_text(s.replace(old, new, 1))

          # 1) RPE: only available 30 min after the session and until the end of that calendar day.
          path = 'react-migration/src/pages/TrainingPageProfessional.jsx'
          replace_once(path,
          """function eventEndTime(event) {
  const explicitEnd = event?.ends_at ? new Date(event.ends_at).getTime() : Number.NaN;
  if (Number.isFinite(explicitEnd)) return explicitEnd;
  const start = event?.starts_at ? new Date(event.starts_at).getTime() : Number.NaN;
  return Number.isFinite(start) ? start + durationMinutes(event) * 60 * 1000 : Number.NaN;
}
""",
          """function eventEndTime(event) {
  const explicitEnd = event?.ends_at ? new Date(event.ends_at).getTime() : Number.NaN;
  if (Number.isFinite(explicitEnd)) return explicitEnd;
  const start = event?.starts_at ? new Date(event.starts_at).getTime() : Number.NaN;
  return Number.isFinite(start) ? start + durationMinutes(event) * 60 * 1000 : Number.NaN;
}

function eventDayEndTime(event) {
  const start = event?.starts_at ? new Date(event.starts_at) : null;
  if (!start || Number.isNaN(start.getTime())) return Number.NaN;
  const endOfDay = new Date(start);
  endOfDay.setHours(23, 59, 59, 999);
  return endOfDay.getTime();
}
""", 'training day end helper')

          replace_once(path,
          """  const endTime = eventEndTime(event);
  const completed = Number.isFinite(endTime) && timeNow >= endTime;
  const rpeAvailable = Number.isFinite(endTime) && timeNow >= endTime + 30 * 60 * 1000;
""",
          """  const endTime = eventEndTime(event);
  const completed = Number.isFinite(endTime) && timeNow >= endTime;
  const rpeDayEnd = eventDayEndTime(event);
  const rpeExpired = Number.isFinite(rpeDayEnd) && timeNow > rpeDayEnd;
  const rpeAvailable = Number.isFinite(endTime) && !rpeExpired && timeNow >= endTime + 30 * 60 * 1000;
""", 'training rpe window')

          replace_once(path,
          """        {!rpeAvailable ? <p className=\"pro-muted-copy\">El RPE se habilitará 30 minutos después de finalizar la sesión.</p> : null}
        {rpeAvailable && loadingExtras ? <p className=\"pro-muted-copy\">Cargando seguimiento…</p> : null}
""",
          """        {rpeExpired ? <p className=\"pro-muted-copy\">El plazo para responder el RPE terminó al finalizar el día del entrenamiento.</p> : null}
        {!rpeExpired && !rpeAvailable ? <p className=\"pro-muted-copy\">El RPE se habilitará 30 minutos después de finalizar la sesión.</p> : null}
        {rpeAvailable && loadingExtras ? <p className=\"pro-muted-copy\">Cargando seguimiento…</p> : null}
""", 'training rpe expired copy')

          # Training in-memory cache: instant re-entry while Supabase revalidates in background.
          replace_once(path,
          """const STATUS = {
  present: { label: 'Presente', short: 'P', icon: UserRoundCheck },
""",
          """const trainingPageCache = new Map();

const STATUS = {
  present: { label: 'Presente', short: 'P', icon: UserRoundCheck },
""", 'training cache declaration')

          replace_once(path,
          """  const isStaff = ['coach', 'administrator'].includes(identity?.profile?.role);
  const isPlayer = identity?.profile?.role === 'player';
  const [teamId, setTeamId] = useState(teams[0]?.id || identity?.player?.team_id || '');
  const [events, setEvents] = useState([]);
  const [attendance, setAttendance] = useState([]);
  const [loading, setLoading] = useState(true);
""",
          """  const isStaff = ['coach', 'administrator'].includes(identity?.profile?.role);
  const isPlayer = identity?.profile?.role === 'player';
  const initialTeamId = teams[0]?.id || identity?.player?.team_id || '';
  const initialCache = initialTeamId ? trainingPageCache.get(initialTeamId) : null;
  const [teamId, setTeamId] = useState(initialTeamId);
  const [events, setEvents] = useState(() => initialCache?.events || []);
  const [attendance, setAttendance] = useState(() => initialCache?.attendance || []);
  const [loading, setLoading] = useState(() => !initialCache);
""", 'training cache state')

          replace_once(path,
          """    setLoading(true);
    setError('');
    try {
""",
          """    const cached = trainingPageCache.get(teamId);
    if (!cached) setLoading(true);
    setError('');
    try {
""", 'training stale while revalidate')

          replace_once(path,
          """      const nextEvents = eventRows || [];
      setEvents(nextEvents);
      const ids = nextEvents.map((event) => event.id);
      if (!ids.length) setAttendance([]);
      else {
        const { data: attendanceRows, error: attendanceError } = await supabase
          .from('attendance')
          .select('event_id,player_id,player_response,official_status,effective_minutes,validated_at')
          .in('event_id', ids);
        if (attendanceError) throw attendanceError;
        setAttendance(attendanceRows || []);
      }
""",
          """      const nextEvents = eventRows || [];
      const ids = nextEvents.map((event) => event.id);
      let nextAttendance = [];
      if (ids.length) {
        const { data: attendanceRows, error: attendanceError } = await supabase
          .from('attendance')
          .select('event_id,player_id,player_response,official_status,effective_minutes,validated_at')
          .in('event_id', ids);
        if (attendanceError) throw attendanceError;
        nextAttendance = attendanceRows || [];
      }
      trainingPageCache.set(teamId, { events: nextEvents, attendance: nextAttendance, cachedAt: Date.now() });
      setEvents(nextEvents);
      setAttendance(nextAttendance);
""", 'training cache write')

          replace_once(path,
          """      setAttendance((current) => [...current.filter((item) => !(item.event_id === data.event_id && item.player_id === data.player_id)), data]);
""",
          """      setAttendance((current) => {
        const next = [...current.filter((item) => !(item.event_id === data.event_id && item.player_id === data.player_id)), data];
        trainingPageCache.set(teamId, { events, attendance: next, cachedAt: Date.now() });
        return next;
      });
""", 'training rsvp cache update')

          replace_once(path,
          """      setEvents((current) => [data, ...current]);
""",
          """      setEvents((current) => {
        const next = [data, ...current];
        trainingPageCache.set(teamId, { events: next, attendance, cachedAt: Date.now() });
        return next;
      });
""", 'training create cache update')

          replace_once(path,
          """      setAttendance((current) => {
        const ids = new Set(saved.map((row) => `${row.event_id}:${row.player_id}`));
        return [...current.filter((row) => !ids.has(`${row.event_id}:${row.player_id}`)), ...saved];
      });
""",
          """      setAttendance((current) => {
        const ids = new Set(saved.map((row) => `${row.event_id}:${row.player_id}`));
        const next = [...current.filter((row) => !ids.has(`${row.event_id}:${row.player_id}`)), ...saved];
        trainingPageCache.set(teamId, { events, attendance: next, cachedAt: Date.now() });
        return next;
      });
""", 'training roll call cache update')

          # 2) Home: expired RPEs never appear; attendance does not flash incorrect buttons.
          path = 'react-migration/src/pages/HomePage.jsx'
          replace_once(path,
          """function writeHomeEventsCache(teamId, training, match) {
  if (!teamId || typeof window === 'undefined') return;
  try {
    window.localStorage.setItem(`${HOME_EVENTS_CACHE_PREFIX}${teamId}`, JSON.stringify({ cachedAt: Date.now(), training: training || null, match: match || null }));
  } catch {
    // La caché es solo una optimización visual; si falla, Supabase sigue siendo la fuente real.
  }
}
""",
          """function writeHomeEventsCache(teamId, training, match) {
  if (!teamId || typeof window === 'undefined') return;
  try {
    window.localStorage.setItem(`${HOME_EVENTS_CACHE_PREFIX}${teamId}`, JSON.stringify({ cachedAt: Date.now(), training: training || null, match: match || null }));
  } catch {
    // La caché es solo una optimización visual; si falla, Supabase sigue siendo la fuente real.
  }
}

const HOME_ATTENDANCE_CACHE_PREFIX = 'volleycoach:home-attendance:';

function readHomeAttendanceCache(playerId, eventId) {
  if (!playerId || !eventId || typeof window === 'undefined') return null;
  try {
    const raw = window.sessionStorage.getItem(`${HOME_ATTENDANCE_CACHE_PREFIX}${playerId}:${eventId}`);
    const value = raw ? JSON.parse(raw) : null;
    return ['yes', 'no'].includes(value?.response) ? value.response : null;
  } catch {
    return null;
  }
}

function writeHomeAttendanceCache(playerId, eventId, response) {
  if (!playerId || !eventId || typeof window === 'undefined') return;
  const key = `${HOME_ATTENDANCE_CACHE_PREFIX}${playerId}:${eventId}`;
  try {
    if (['yes', 'no'].includes(response)) window.sessionStorage.setItem(key, JSON.stringify({ response, cachedAt: Date.now() }));
    else window.sessionStorage.removeItem(key);
  } catch {
    // Optimización visual únicamente.
  }
}
""", 'home attendance cache helpers')

          replace_once(path,
          """  const initialHomeEvents = useMemo(() => readHomeEventsCache(team?.id), [team?.id]);

  const [loading, setLoading] = useState(() => !initialHomeEvents);
""",
          """  const initialHomeEvents = useMemo(() => readHomeEventsCache(team?.id), [team?.id]);
  const initialAttendanceResponse = useMemo(
    () => readHomeAttendanceCache(identity?.player?.id, initialHomeEvents?.training?.id),
    [identity?.player?.id, initialHomeEvents?.training?.id]
  );

  const [loading, setLoading] = useState(() => !initialHomeEvents);
""", 'home initial attendance cache')

          replace_once(path,
          """  const [trainingAttendance, setTrainingAttendance] = useState([]);
  const [playerAttendanceResponse, setPlayerAttendanceResponse] = useState(null);
  const [attendanceSaving, setAttendanceSaving] = useState(false);
""",
          """  const [trainingAttendance, setTrainingAttendance] = useState([]);
  const [playerAttendanceResponse, setPlayerAttendanceResponse] = useState(() => initialAttendanceResponse);
  const [playerAttendanceLoaded, setPlayerAttendanceLoaded] = useState(() => isStaff || Boolean(initialAttendanceResponse));
  const [attendanceSaving, setAttendanceSaving] = useState(false);
""", 'home attendance loaded state')

          replace_once(path,
          """                if (!Number.isFinite(end) || now.getTime() < end + 30 * 60 * 1000) return false;
                if (ownPlayerRpeEventIds.has(event.id)) return false;
""",
          """                if (!Number.isFinite(end) || now.getTime() < end + 30 * 60 * 1000) return false;
                const trainingDay = new Date(event.starts_at);
                const dayEnd = new Date(trainingDay);
                dayEnd.setHours(23, 59, 59, 999);
                if (!Number.isFinite(dayEnd.getTime()) || now.getTime() > dayEnd.getTime()) return false;
                if (ownPlayerRpeEventIds.has(event.id)) return false;
""", 'home expired pending rpe filter')

          replace_once(path,
          """      if (isStaff || !identity?.player?.id || !nextTraining?.id) {
        if (active) {
          setPlayerAttendanceResponse(null);
          setAttendanceError('');
        }
        return;
      }

      const { data, error: ownAttendanceError } = await supabase
""",
          """      if (isStaff || !identity?.player?.id || !nextTraining?.id) {
        if (active) {
          setPlayerAttendanceResponse(null);
          setPlayerAttendanceLoaded(true);
          setAttendanceError('');
        }
        return;
      }

      const cachedResponse = readHomeAttendanceCache(identity.player.id, nextTraining.id);
      if (active) {
        setPlayerAttendanceResponse(cachedResponse);
        setPlayerAttendanceLoaded(Boolean(cachedResponse));
      }

      const { data, error: ownAttendanceError } = await supabase
""", 'home attendance preload before query')

          replace_once(path,
          """      if (ownAttendanceError) {
        setAttendanceError('No se pudo cargar tu respuesta de asistencia.');
        return;
      }
      setPlayerAttendanceResponse(data?.player_response || null);
      setAttendanceError('');
""",
          """      if (ownAttendanceError) {
        setPlayerAttendanceLoaded(true);
        setAttendanceError('No se pudo cargar tu respuesta de asistencia.');
        return;
      }
      const response = data?.player_response || null;
      setPlayerAttendanceResponse(response);
      setPlayerAttendanceLoaded(true);
      writeHomeAttendanceCache(identity.player.id, nextTraining.id, response);
      setAttendanceError('');
""", 'home attendance query settle')

          replace_once(path,
          """      setPlayerAttendanceResponse(data?.player_response || response);
""",
          """      const savedResponse = data?.player_response || response;
      setPlayerAttendanceResponse(savedResponse);
      setPlayerAttendanceLoaded(true);
      writeHomeAttendanceCache(identity.player.id, displayNextTraining.id, savedResponse);
""", 'home attendance save cache')

          replace_once(path,
          """      setWellness((rows) => [data, ...rows.filter((row) => row.entry_date !== todayKey)]);
      setCheckinOpen(false);
""",
          """      setWellness((rows) => [data, ...rows.filter((row) => row.entry_date !== todayKey)]);
      window.dispatchEvent(new CustomEvent('volleycoach:wellness-updated', { detail: { playerId: identity.player.id, entryDate: todayKey } }));
      setCheckinOpen(false);
""", 'home wellness success event')

          replace_once(path,
          """        setCheckinOpen(false);
        setWellness((rows) => rows.some((row) => row.entry_date === todayKey) ? rows : [{ player_id: identity.player.id, entry_date: todayKey, fatigue: checkinFatigue, sleep: checkinSleep, pain_score: checkinPain, notes: checkinNotes.trim() }, ...rows]);
""",
          """        setCheckinOpen(false);
        setWellness((rows) => rows.some((row) => row.entry_date === todayKey) ? rows : [{ player_id: identity.player.id, entry_date: todayKey, fatigue: checkinFatigue, sleep: checkinSleep, pain_score: checkinPain, notes: checkinNotes.trim() }, ...rows]);
        window.dispatchEvent(new CustomEvent('volleycoach:wellness-updated', { detail: { playerId: identity.player.id, entryDate: todayKey } }));
""", 'home wellness duplicate event')

          replace_once(path,
          """                <div className=\"player-home-attendance-actions\" aria-label=\"Confirma tu asistencia al próximo entrenamiento\">
                  <button type=\"button\" className={`player-home-attendance-btn yes ${playerAttendanceResponse === 'yes' ? 'active' : ''}`} disabled={attendanceSaving} onClick={() => void saveOwnAttendance('yes')}><CheckCircle2 size={17} /> Sí, asistiré</button>
                  <button type=\"button\" className={`player-home-attendance-btn no ${playerAttendanceResponse === 'no' ? 'active' : ''}`} disabled={attendanceSaving} onClick={() => void saveOwnAttendance('no')}><XCircle size={17} /> No asistiré</button>
                </div>
                {attendanceError ? <div className=\"player-home-attendance-error\">{attendanceError}</div> : null}
                {playerAttendanceResponse ? (
""",
          """                {playerAttendanceLoaded ? (
                  <div className=\"player-home-attendance-actions\" aria-label=\"Confirma tu asistencia al próximo entrenamiento\">
                    <button type=\"button\" className={`player-home-attendance-btn yes ${playerAttendanceResponse === 'yes' ? 'active' : ''}`} disabled={attendanceSaving} onClick={() => void saveOwnAttendance('yes')}><CheckCircle2 size={17} /> Sí, asistiré</button>
                    <button type=\"button\" className={`player-home-attendance-btn no ${playerAttendanceResponse === 'no' ? 'active' : ''}`} disabled={attendanceSaving} onClick={() => void saveOwnAttendance('no')}><XCircle size={17} /> No asistiré</button>
                  </div>
                ) : <div className=\"player-home-attendance-loading\" aria-label=\"Comprobando tu respuesta de asistencia\"><span /></div>}
                {attendanceError ? <div className=\"player-home-attendance-error\">{attendanceError}</div> : null}
                {playerAttendanceLoaded && playerAttendanceResponse ? (
""", 'home attendance flash guard render')

          # Home loading placeholder styling.
          css = Path('react-migration/src/pages/HomePageDashboard.css')
          css_text = css.read_text()
          marker = '/* player-attendance-loading-20260902 */'
          if marker not in css_text:
              css_text += """

/* player-attendance-loading-20260902 */
.player-home-attendance-loading{height:48px;border-radius:14px;border:1px solid rgba(255,255,255,.12);background:rgba(255,255,255,.06);overflow:hidden;position:relative}
.player-home-attendance-loading span{position:absolute;inset:0;transform:translateX(-100%);background:linear-gradient(90deg,transparent,rgba(255,255,255,.10),transparent);animation:playerAttendanceSoftLoad 1.15s ease-in-out infinite}
@keyframes playerAttendanceSoftLoad{to{transform:translateX(100%)}}
"""
              css.write_text(css_text)

          # 3) Wellness auxiliary check-in always refreshes when the player actually enters Wellness.
          path = 'react-migration/src/components/WellnessPlayerCheckin.jsx'
          replace_once(path,
          """  useEffect(() => {
    void refresh();
  }, [refresh]);

  function openCheckin() {
""",
          """  useEffect(() => {
    void refresh();
  }, [refresh]);

  useEffect(() => {
    if (host) void refresh();
  }, [host, refresh]);

  useEffect(() => {
    const sync = () => { void refresh(); };
    const onVisibility = () => { if (document.visibilityState === 'visible') void refresh(); };
    window.addEventListener('volleycoach:wellness-updated', sync);
    document.addEventListener('visibilitychange', onVisibility);
    return () => {
      window.removeEventListener('volleycoach:wellness-updated', sync);
      document.removeEventListener('visibilitychange', onVisibility);
    };
  }, [refresh]);

  function openCheckin() {
""", 'wellness checkin refresh listeners')

          replace_once(path,
          """      setHasToday(true);
      setSaved(true);
      window.dispatchEvent(new CustomEvent('volleycoach:wellness-updated'));
      window.setTimeout(() => window.location.reload(), 650);
""",
          """      setHasToday(true);
      setSaved(true);
      setOpen(false);
      window.dispatchEvent(new CustomEvent('volleycoach:wellness-updated', { detail: { playerId, entryDate: localDateKey() } }));
""", 'wellness checkin remove reload')

          # Wellness page in-memory cache + background refresh on updates.
          path = 'react-migration/src/pages/WellnessPage.jsx'
          replace_once(path,
          """import './WellnessPage.css';

function localDateKey(date = new Date()) {
""",
          """import './WellnessPage.css';

const wellnessPageCache = new Map();

function localDateKey(date = new Date()) {
""", 'wellness cache declaration')

          replace_once(path,
          """  const currentPlayerId = identity?.player?.id || null;
  const today = localDateKey();

  const [loading, setLoading] = useState(true);
  const [error, setError] = useState('');
  const [players, setPlayers] = useState([]);
  const [wellnessRows, setWellnessRows] = useState([]);
  const [trainingEvents, setTrainingEvents] = useState([]);
  const [rpeRows, setRpeRows] = useState([]);
  const [attendanceRows, setAttendanceRows] = useState([]);
""",
          """  const currentPlayerId = identity?.player?.id || null;
  const today = localDateKey();
  const cacheKey = `${team?.id || 'no-team'}:${isStaff ? 'staff' : currentPlayerId || 'player'}`;
  const initialCache = wellnessPageCache.get(cacheKey) || null;

  const [loading, setLoading] = useState(() => !initialCache);
  const [error, setError] = useState('');
  const [players, setPlayers] = useState(() => initialCache?.players || []);
  const [wellnessRows, setWellnessRows] = useState(() => initialCache?.wellnessRows || []);
  const [trainingEvents, setTrainingEvents] = useState(() => initialCache?.trainingEvents || []);
  const [rpeRows, setRpeRows] = useState(() => initialCache?.rpeRows || []);
  const [attendanceRows, setAttendanceRows] = useState(() => initialCache?.attendanceRows || []);
""", 'wellness cache state')

          replace_once(path,
          """    setLoading(true);
    setError('');
    try {
""",
          """    if (!wellnessPageCache.has(cacheKey)) setLoading(true);
    setError('');
    try {
""", 'wellness stale while revalidate')

          replace_once(path,
          """      setPlayers(nextPlayers);
      setTrainingEvents(eventResult.data || []);
      setWellnessRows(wellnessResult.data || []);
      setRpeRows(rpeResult.data || []);
      setAttendanceRows(attendanceResult.data || []);
""",
          """      const snapshot = {
        players: nextPlayers,
        trainingEvents: eventResult.data || [],
        wellnessRows: wellnessResult.data || [],
        rpeRows: rpeResult.data || [],
        attendanceRows: attendanceResult.data || [],
        cachedAt: Date.now()
      };
      wellnessPageCache.set(cacheKey, snapshot);
      setPlayers(snapshot.players);
      setTrainingEvents(snapshot.trainingEvents);
      setWellnessRows(snapshot.wellnessRows);
      setRpeRows(snapshot.rpeRows);
      setAttendanceRows(snapshot.attendanceRows);
""", 'wellness cache write')

          replace_once(path,
          """  }, [currentPlayerId, isStaff, team?.id]);

  useEffect(() => {
    void loadData();
  }, [loadData]);
""",
          """  }, [cacheKey, currentPlayerId, isStaff, team?.id]);

  useEffect(() => {
    void loadData();
  }, [loadData]);

  useEffect(() => {
    const onUpdated = () => { void loadData(); };
    window.addEventListener('volleycoach:wellness-updated', onUpdated);
    return () => window.removeEventListener('volleycoach:wellness-updated', onUpdated);
  }, [loadData]);
""", 'wellness live update listener')

          # 4) Calendar month cache: revisit current month without a blank loading state.
          path = 'react-migration/src/pages/CalendarPage.jsx'
          replace_once(path,
          """const WEEKDAYS = ['L', 'M', 'X', 'J', 'V', 'S', 'D'];
""",
          """const calendarPageCache = new Map();

const WEEKDAYS = ['L', 'M', 'X', 'J', 'V', 'S', 'D'];
""", 'calendar cache declaration')

          replace_once(path,
          """  const isStaff = ['coach', 'administrator'].includes(identity?.profile?.role);
  const today = useMemo(() => new Date(), []);
  const [cursor, setCursor] = useState(() => new Date(today.getFullYear(), today.getMonth(), 1, 12));
  const [selectedKey, setSelectedKey] = useState(() => dateKey(today));
  const [events, setEvents] = useState([]);
  const [birthdays, setBirthdays] = useState([]);
  const [leagueTeams, setLeagueTeams] = useState([]);
  const [loading, setLoading] = useState(true);
""",
          """  const isStaff = ['coach', 'administrator'].includes(identity?.profile?.role);
  const today = useMemo(() => new Date(), []);
  const teamCachePrefix = teamIds.join(',');
  const initialMonthCacheKey = `${teamCachePrefix}:${today.getFullYear()}-${today.getMonth()}`;
  const initialMonthCache = calendarPageCache.get(initialMonthCacheKey) || null;
  const [cursor, setCursor] = useState(() => new Date(today.getFullYear(), today.getMonth(), 1, 12));
  const [selectedKey, setSelectedKey] = useState(() => dateKey(today));
  const [events, setEvents] = useState(() => initialMonthCache?.events || []);
  const [birthdays, setBirthdays] = useState(() => initialMonthCache?.birthdays || []);
  const [leagueTeams, setLeagueTeams] = useState(() => initialMonthCache?.leagueTeams || []);
  const [loading, setLoading] = useState(() => !initialMonthCache);
""", 'calendar cache state')

          replace_once(path,
          """      if (!active) return;
      if (!error) setLeagueTeams(data || []);
""",
          """      if (!active) return;
      if (!error) {
        const nextLeagueTeams = data || [];
        setLeagueTeams(nextLeagueTeams);
        const monthKey = `${teamCachePrefix}:${cursor.getFullYear()}-${cursor.getMonth()}`;
        const cached = calendarPageCache.get(monthKey) || {};
        calendarPageCache.set(monthKey, { ...cached, leagueTeams: nextLeagueTeams });
      }
""", 'calendar league cache')

          replace_once(path,
          """      setLoading(true);
      setLoadError('');
      try {
""",
          """      const monthCacheKey = `${teamCachePrefix}:${cursor.getFullYear()}-${cursor.getMonth()}`;
      const cachedMonth = calendarPageCache.get(monthCacheKey);
      if (cachedMonth) {
        setEvents(cachedMonth.events || []);
        setBirthdays(cachedMonth.birthdays || []);
        if (cachedMonth.leagueTeams?.length) setLeagueTeams(cachedMonth.leagueTeams);
        setLoading(false);
      } else {
        setLoading(true);
      }
      setLoadError('');
      try {
""", 'calendar stale while revalidate')

          replace_once(path,
          """        setBirthdays(nextBirthdays);
      } catch (error) {
""",
          """        setBirthdays(nextBirthdays);
        const cachedLeagueTeams = calendarPageCache.get(monthCacheKey)?.leagueTeams || leagueTeams;
        calendarPageCache.set(monthCacheKey, { events: eventResult.data || [], birthdays: nextBirthdays, leagueTeams: cachedLeagueTeams, cachedAt: Date.now() });
      } catch (error) {
""", 'calendar cache write')

          replace_once(path,
          """  }, [cursor, teamIds]);
""",
          """  }, [cursor, teamCachePrefix, teamIds]);
""", 'calendar load dependencies')

          # 5) Core tabs are eagerly bundled; secondary screens warm in idle time.
          path = 'react-migration/src/App.jsx'
          replace_once(path,
          """import LoginPage from './pages/LoginPage.jsx';
import ProfileModal from './components/ProfileModal.jsx';

const HomePage = lazy(() => import('./pages/HomePage.jsx'));
const TrainingPage = lazy(() => import('./pages/TrainingPage.jsx'));
const CalendarPage = lazy(() => import('./pages/CalendarPage.jsx'));
const WellnessPage = lazy(() => import('./pages/WellnessPage.jsx'));
const RosterPage = lazy(() => import('./pages/RosterPage.jsx'));
""",
          """import LoginPage from './pages/LoginPage.jsx';
import ProfileModal from './components/ProfileModal.jsx';
import HomePage from './pages/HomePage.jsx';
import TrainingPage from './pages/TrainingPage.jsx';
import CalendarPage from './pages/CalendarPage.jsx';
import WellnessPage from './pages/WellnessPage.jsx';

const RosterPage = lazy(() => import('./pages/RosterPage.jsx'));
""", 'app eager core imports')

          replace_once(path,
          """  useEffect(() => {
    let active = true;
    if (!avatarPath) {
""",
          """  useEffect(() => {
    if (!identity?.profile?.id) return undefined;
    const warmSecondary = () => {
      void Promise.allSettled([
        import('./pages/RosterPage.jsx'),
        import('./pages/StatisticsPage.jsx'),
        import('./pages/CompetitionPage.jsx'),
        import('./pages/GamePlanPage.jsx'),
        import('./pages/PerformancePage.jsx')
      ]);
    };
    if ('requestIdleCallback' in window) {
      const idleId = window.requestIdleCallback(warmSecondary, { timeout: 1800 });
      return () => window.cancelIdleCallback?.(idleId);
    }
    const timer = window.setTimeout(warmSecondary, 900);
    return () => window.clearTimeout(timer);
  }, [identity?.profile?.id]);

  useEffect(() => {
    let active = true;
    if (!avatarPath) {
""", 'app idle warm secondary modules')

          print('All patches applied')
          PY

      - name: Commit fixes
        run: |
          if git diff --quiet; then
            echo "No changes needed"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add react-migration/src/App.jsx \
                  react-migration/src/pages/HomePage.jsx \
                  react-migration/src/pages/HomePageDashboard.css \
                  react-migration/src/pages/TrainingPageProfessional.jsx \
                  react-migration/src/pages/WellnessPage.jsx \
                  react-migration/src/pages/CalendarPage.jsx \
                  react-migration/src/components/WellnessPlayerCheckin.jsx
          git commit -m "Fix daily response windows and smooth core navigation"
          git push origin HEAD:react-vite-migration
