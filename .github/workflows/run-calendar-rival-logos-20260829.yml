name: Run calendar rival logos 20260829
on:
  push:
    branches: [react-vite-migration]
    paths:
      - '.github/workflows/run-calendar-rival-logos-20260829.yml'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: react-vite-migration
      - run: python react-migration/patch_calendar_rivals_20260829.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add react-migration/src/pages/CalendarPage.jsx react-migration/src/pages/CalendarPage.css
          git commit -m "Añadir escudos de rivales al calendario" || exit 0
          git push origin react-vite-migration
